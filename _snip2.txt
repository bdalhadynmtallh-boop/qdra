                {aiSaveLoading ? "ط¬ط§ط±ظچ ط§ظ„ط­ظپط¸..." : "ط­ظپط¸ ط§ظ„ط¥ط¹ط¯ط§ط¯ط§طھ"}
              </button>
            </>
          )}

        </main>

      </div>

    </div>
  );
}

/* =========================================================
   SIDEBAR BUTTON
========================================================= */

function SidebarButton({
  active,
  icon,
  label,
  badge,
  onClick,
